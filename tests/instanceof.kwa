var segment s;
var point p;

class point {
  attribute bool rouge ;
  attribute int x;
  attribute int y;

  method void constructor(int a, int b, bool r) {
    this.x = a;
    this.y = b;
    this.rouge = r;
  }
}

class segment {
  attribute point a;
  attribute point b;

  method void constructor(point p1, point p2) {
    this.a = p1;
    this.b = p2;
  }
}

main {
  p = new point(1,2,true);
  s = new segment(p, new point(4,7,false));
  var point c;
  if( !(p instanceof void)&& !( p.rouge instanceof void) ){
    if(s instanceof segment && !(s instanceof point)){
        if(p.x instanceof int && p.y instanceof int){
            if(p.rouge instanceof bool){
                if(!(c.x instanceof bool)){
                    print(1);
                }
            }
        }
    }
  }
}
